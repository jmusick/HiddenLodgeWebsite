# AGENTS.md

Guidance for AI coding agents working in this repository.

## Stack

- **Astro 6** (SSR, `output: "server"`) deployed to **Cloudflare Pages/Workers** via `@astrojs/cloudflare`.
- **D1** (Cloudflare's SQLite) as the database, binding name `DB`. Schema lives as sequential SQL files in `migrations/*.sql` — there is no ORM.
- Pages (file-based routing) live in `src/pages/`. API endpoints are Astro endpoints under `src/pages/api/**/*.ts`, each exporting `POST`/`GET`/etc. and `export const prerender = false`.
- Shared server-side logic lives in `src/lib/**` (plain `.ts`, imported by both pages and API routes).
- Layouts: `src/layouts/Layout.astro` (public site chrome + nav) and `src/layouts/AdminLayout.astro` (admin nav/tabs).

## Conventions

- **Auth checks**: pages check `Astro.locals.isAdmin` / `Astro.locals.isGuildMember` / `Astro.locals.user` and `return Astro.redirect(...)` early if unauthorized. API routes check `context.locals.isAdmin` etc. and return `new Response('Forbidden', { status: 403 })` / `401` early in the handler.
- **Feature flags**: `src/lib/feature-flags.ts` exports `FEATURE_FLAGS`, a plain object of booleans (one per major feature: `rosterTeams`, `raidSignups`, `attendance`, `deathAnalysis`, `mechanicsAnalysis`, `applications`, `feedback`, `tools`, `sim`). A feature that's temporarily disabled but not removed is gated everywhere it surfaces:
  - Nav arrays (`AdminLayout.astro`'s `adminModules`, `Layout.astro`'s `memberNavItems`) conditionally include the entry.
  - Pages add `if (!FEATURE_FLAGS.x) return Astro.redirect('/')` (public) or `return Astro.redirect('/admin')` (admin), placed after the existing auth guard.
  - API routes add `if (!FEATURE_FLAGS.x) return new Response('Not found', { status: 404 })`, placed after the existing auth guard.
  - Flip the flag back to `true` to fully restore a feature — no other code changes needed. Underlying `src/lib/*.ts` business logic and DB schema for disabled features are left intact on purpose.
- **D1 queries**: written as raw SQL via `env.DB.prepare(...).bind(...).all()/.first()/.run()`. `env` is imported from `cloudflare:workers`.
- Migrations are numbered and additive (`NNNN_description.sql`); never edit an already-applied migration — add a new one.
- **Weekly reset / season timestamps**: `src/lib/wow-reset.ts` is the single source of truth for the US weekly reset (Tuesday 11:00 AM America/New_York, DST-aware) and season-start timestamps (e.g. `SEASON_2_START_TIMESTAMP`). This used to be copy-pasted independently in three files with a wrong reset hour — always import from here rather than reimplementing it.

## Commands

- `npm run dev` — Astro dev server + local cron refresher (see `scripts/dev-with-cron.mjs`).
- `npm run build` — `astro build` then patches the Wrangler config. It does **not** typecheck.
- `npm run typecheck` — `astro check`. The repo is clean (0 errors, 0 warnings); keep it that way. The remaining hints are known false positives — values used only inside `<script type="application/json">` or inline `on*` attributes (`RaiderSimTools.astro`, `signup.astro`, `trinkets.astro`) — plus style suggestions, so don't "fix" those by deleting code.
- `npm run db:migrate:local` / `db:migrate:prod` — apply migrations. Destructive migrations (DROP/DELETE/TRUNCATE/`ALTER TABLE ... DROP COLUMN`) are blocked unless the file has a `-- allow-destructive` annotation; `npm run db:check:migrations` verifies this plus migration numbering/state without touching a database.
- `npm run db:copy-prod` — copies all production D1 data into your local D1 (schema intersection only; skips `sessions`, system tables, and by default `roster_cache_meta`/`site_settings`). Requires `wrangler` to be authenticated (`npx wrangler whoami`) and a bootstrapped local schema (`npm run db:bootstrap:local`). Safe to rerun — it clears local rows (FK-dependency order) before reinserting. Set `COPY_PROD_INCLUDE_SEEDED=1` in the environment to also copy the normally-skipped seeded tables.

## Notes

- **Cron**: the site is a Cloudflare Pages project, which has no cron triggers, so there is no `[triggers] crons` entry and no `scheduled()` handler. cron-job.org calls the endpoints with an `X-Cron-Secret` header and aborts each request at 30s. Production runs three jobs: `/api/cron/refresh?skipRaiders=1&skipLogs=1` (roster, gear icons, professions/trinkets/attendance when their flags are on), `/api/cron/refresh-raiders`, and `/api/cron/refresh-logs` (raid-log activity + death analysis, then mechanics analysis). Each leg must fit inside 30s on its own: take a deadline at the start, check it before starting each new unit of work, carry the remainder over to the next tick, and cap every upstream request (`AbortSignal.timeout(8_000)` in `blizzard-fetch.ts`, `raider-io.ts`, `wcl.ts`, `raid-log-activity.ts`). When adding a slow leg, give it its own endpoint plus a `skip*` flag on `/api/cron/refresh` instead of adding it to the shared request.
- **Recent-raider filter**: `src/lib/raid-log-activity.ts` syncs "who showed up in a Hidden Lodge Warcraft Logs report lately" straight from the guild's WCL report list (guild 781707) and stores it in `raider_log_activity`. It is deliberately independent of `src/lib/attendance.ts` — that pipeline is tied to scheduled raids and stays empty while `FEATURE_FLAGS.attendance` is off — and so it is **not** gated on that flag. `/raiders` and `/raiders/gear-summary` default to the last `RECENT_LOG_WINDOW_DAYS` (30) and offer a "Show all raiders" toggle; with no synced data both pages fail open and show everyone. `raid_log_reports_seen` is the don't-refetch cursor, so a caught-up refresh costs one GraphQL call.
- **Death Analysis (Season 2)**: `src/lib/death-analysis.ts` backs `/death-analysis` (Tools menu, guild members) and `/admin/log-matching` (admins + officers), gated on `FEATURE_FLAGS.deathAnalysis`. It pulls the WCL guild report list directly — no dependency on `raid_signups`/`primary_raid_schedules` — and keeps reports in The Venomous Abyss that overlap a Thu/Fri 9pm–midnight ET window (DST-aware via `easternWallClockToUtcSeconds` in `wow-reset.ts`), counting only Heroic/Mythic pulls of 30+ seconds (`MIN_FIGHT_DURATION_MS`; excludes instant wipes) over a rolling 90 days. One report counts per night (`night_key` = ET date): officer override from `death_analysis_night_overrides`, else most qualifying pulls. The sync runs from `/api/cron/refresh-logs`, a few reports per run (new reports, plus reports whose WCL `endTime` is past their `synced_at`, i.e. live logs synced mid-raid; re-syncs keep the stored `night_key`, and mechanics re-sync when their `synced_at` predates the report's), and stops starting new reports once its 12s budget is spent. Alongside the per-report `death_analysis_stats` aggregate, `syncReport` also persists one row per counted death (capped at the first four per pull, same as the scoring cap) in `death_analysis_events` — report code, fight id, encounter name, and offset into the pull — so `/death-analysis` can expand a raider row into direct `warcraftlogs.com/reports/<code>#fight=<id>&type=deaths&death=<position>` links that jump straight to that death's moment (WCL's own `death=` param, confirmed by clicking a death row on a live report — it's the fight-wide death ordinal, exactly what we already store as `death_position`). The old `attendance` pipeline (`src/lib/attendance.ts`, `raid_attendance_*` tables) is retired and stays off; don't wire new work into it.
- **Bench**: `src/lib/bench.ts` ranks Death Analysis's qualified raiders by a combined score of median WCL parse and death percentile. It has no page of its own — it backs the "Configuration Settings" panel on `/raid-composition` (see below), gated on `FEATURE_FLAGS.deathAnalysis`. The scoring and the parse/deaths weight slider and parse-scale toggle (guild percentile vs raw parse) run client-side from JSON embedded in the page; this ordering is a live preview, not saved until an officer clicks "Save & Regenerate Comp". The panel's "Roster" table groups raiders by their effective role (Tanks/Healers/Ranged DPS/Melee DPS, best first in each) rather than one flat list, with all 4 role tables sharing the same `<colgroup>` column widths (`table-layout: fixed`, `.roster-table`) so they line up as if they were one table. Parses come from WCL `characterData.character(...).zoneRankings` (Heroic, `byBracket`, one median per role: DPS and tanks by damage, healers by healing) and are cached in `bench_parses`; the raid zone is read from the newest counted Death Analysis report. Role (tank/healer/dps) is whichever role has the most ranked bosses unless an officer sets one in `bench_role_overrides` (`POST /api/admin/bench/role-override`, redirects back to `/raid-composition`). Within DPS, melee-vs-ranged is a separate override in `bench_flags` (`POST /api/admin/bench/set-flag`, `field=melee_ranged`) for classes WCL's spec data hasn't caught up on yet (e.g. an Enhancement Shaman auto-detected as ranged from the class-default fallback). `bench_flags` also holds two officer toggles that gate Raid Comp's Regenerate: `is_absent` (never picked) and `is_raid_leader` (always picked, bumping a lower-priority same-role pick if needed) — same one-endpoint/one-table pattern, `field=absent` / `field=raid_leader`. Parses (and mechanic roles, see below) refresh from `/api/cron/refresh-logs` (chained after log activity, only for rows older than `BENCH_PARSE_STALE_SECONDS`) and from the page's Refresh Parses button (`POST /api/admin/bench/refresh`). Raiders under the Death Analysis pull/report minimum, or with no parse in their role, are listed separately (Not Scored) and never included in the comp.
- **Raid Comp**: `/raid-composition` (Tools menu, guild members; gated on `FEATURE_FLAGS.tools`, `deathAnalysis`, and `raidComp`), backed by `src/lib/raid-comp.ts`. A shared, officer-maintained 20-man suggested comp built from Bench's priority order: everyone sees the same board (groups 1–4, a "Bench" list of unassigned scored raiders — both grouped into Tanks/Healers/Ranged DPS/Melee DPS — and a live coverage panel — roles, raid buffs, utility (with per-item minimum-count inputs), healer cooldowns via `computeTeamSummary`/`ALL_RAID_BUFFS`/`ALL_UTILITY`/`classRaidData` from `src/lib/raid-teams.ts`, pure TS and safe to import into the browser bundle; armor tokens are deliberately not shown here, unlike Roster Teams' summary), plus a "Configuration Settings" `<details>` panel (Roster/Not Scored tables, the weight/scale/quota controls, Refresh Parses) that's likewise visible to every guild member but view-only for them: every mutating control in there (quota inputs, weight slider, scale radios, per-raider Role/Melee-Ranged/Absent/RL controls, Refresh Parses, Save & Regenerate) is server-rendered `disabled` for non-officers, on top of the mutation endpoints themselves rejecting non-officers. Officers (`Astro.locals.isOfficer`) additionally get drag-and-drop and the remove/add controls on the board, plus an "Absent" toggle button right on each chip (both benched and already-placed) that POSTs to `/api/admin/bench/set-flag` via `fetch` (not a real form submission/reload, unlike the Roster table's own checkboxes) and optimistically mutates the shared `Raider` object in place — since `chipElement`/`renderComp`/the Roster table all read from the same `scored` array of objects (never cloned), that mutation is visible everywhere immediately; `refreshRoster` (a mutable top-level `let`, assigned once the Configuration Settings block defines `renderRoster`) is also called so the Roster table's own checkbox doesn't go stale. A "Reset Absent" button next to the Bench section's Regenerate button (`POST /api/admin/bench/reset-absent`, form submit + reload like the other `bench_flags` mutations, not fetch-based) clears everyone's Absent flag at once — for starting a fresh raid night without unchecking each person. Every bench chip (visible to everyone, not just officers) also gets a "?" button that expands a small popover explaining why that raider isn't currently in the comp — their rank within their role bucket by the *live* weight/scale from the Configuration Settings inputs (`computeScoredRows()`, shared by both this popover and the Roster table so the numbers always agree), whether their raid buffs are already covered elsewhere in the comp ("buff-redundant") or would actually add coverage nobody currently provides, and the same for any utility item they provide that currently has an officer-set minimum. "Save & Regenerate Comp" appears in three places — the Configuration Settings panel and a header button on both the Raid Groups and Bench sections — all wired to one `regenerateComp()` function so there's a single confirm dialog and disable-during-request behavior. A separate "Clear Comp" button in the Raid Groups header POSTs to `POST /api/raid-composition/clear` (`clearRaidComp`), which deletes `raid_comp_assignments` + `raid_comp_manual_overrides` — the same two tables Regenerate rewrites — but inserts nothing, so the board empties to the bench while quotas, weights, utility minimums, Absent/RL flags, and saved loadouts survive; unlike Regenerate it deliberately leaves `CONFIG_SETTINGS_OPEN_KEY` in place so the settings panel stays expanded. Regenerate itself POSTs to `POST /api/raid-composition/regenerate`, which saves the quota/weight/scale inputs to the `raid_comp_settings` singleton row and replaces the whole assignment set (`buildAssignments` in `raid-comp.ts`):
  1. Drop `isAbsent` raiders from the candidate pool entirely; `isRaidLeader` raiders are pulled to the front of their role's pool first (`withForced`), so they're picked even if a normal cutoff would've excluded them, bumping the lowest-priority non-RL pick.
  2. Fill tank/healer/DPS quotas (default 2/5/13) from what's left, interleaving melee/ranged DPS by priority so both fill evenly.
  3. `improveCoverage` does one pass over a combined requirements list — every entry in `ALL_RAID_BUFFS` (implicit minimum 1) plus every utility item with an officer-set minimum > 0 in `raid_comp_utility_minimums` (0 = no requirement, so most utility items are skipped by default): for each requirement not yet at its minimum, it looks for the best unpicked candidate who provides it and swaps them in for the *worst* current pick in that candidate's role bucket, repeating up to that requirement's own minimum (so "2 Demonic Gateways" can trigger two swaps) — but only if that pick isn't required to stay for some *other* requirement's own minimum, and never a raid leader. A requirement with no safe swap available is left short (best-effort, not guaranteed) — the coverage panel reports whatever's still missing/short.
  4. Group placement is not a plain round-robin: tanks alternate strictly between groups 1 and 2 only; healers and DPS round-robin across all 4 groups (healers land one per group before doubling up; melee/ranged DPS fill whatever room is left as evenly as possible). Every group is capped at `MAX_GROUP_SIZE` (5); once every eligible group for a bucket is full, remaining picks are left unassigned (benched).

  **Export to Addon**: the Raid Groups header's "Export to Addon" button (visible to every guild member, since it's read-only) builds a client-side string from the same `assignments`/`raiderById` the board already renders — no API endpoint involved — and shows it in a `<dialog>` (`#export-dialog`) to copy. Format: `HLRC1|Name-Realm:Group:CLASSTOKEN,...`, one entry per placed raider (bench is excluded, nothing to do for them); a temp/PUG candidate has no real realm so it's emitted as bare `Name:Group:CLASSTOKEN`. The realm is stripped of spaces/hyphens/apostrophes (`normalizeRealmForExport`) so it can't collide with the `-` separator — this mirrors the normalization the Hidden Lodge addon and Desktop Bridge already do elsewhere (see `HiddenLodge/HiddenLodge_InviteTool.lua`'s `normalizeRealm`). `classTokenForExport` upper-cases and strips spaces from `className` (e.g. `Death Knight` → `DEATHKNIGHT`) to match Blizzard's `RAID_CLASS_COLORS` keys, so the addon can class-color rows without needing its own name table. The counterpart lives in the separate `HiddenLodge` addon repo (`HiddenLodge_RaidComp.lua`, not this one): its Raid Comp panel (`/hl raidcomp`) parses this pasted string (no desktop sync or `/reload` involved, since the comp needs to be current right before pulling) and uses it to invite raiders not yet in the raid and move already-grouped raiders into their assigned subgroup (`SetRaidSubgroup`/`C_PartyInfo.SwapRaidSubgroup`, best-effort — see that repo's README).

  Melee/ranged (absent WCL data or an override) comes from `bench_mechanic_roles` (`refreshBenchMechanicRoles` in `bench.ts`, same refresh cadence as parses), which derives each raider's spec from WCL `CombatantInfo` on the latest canonical raid night via `ROLE_BY_SPEC_ID` (exported from `mechanics-analysis.ts`); a raider `bench_mechanic_roles` hasn't seen yet falls back to a static per-class default (ambiguous classes — Druid, Shaman — default to ranged) unless overridden in `bench_flags`. `naturalRole()`/`meleeOrRanged()` (a raider's fixed tank/healer/melee/ranged slot) are intentionally duplicated between `raid-comp.ts` (server, used by regenerate) and the page's client script (used for rendering/interaction) rather than shared, since one runs in a Worker and the other in the browser bundle — keep both in sync when the override/fallback logic changes.

  **Buffs vs Utility**: `src/lib/raid-teams.ts`'s `ClassRaidData` splits `buffs` (stat buffs, Bloodlust, and the two boss debuffs Chaos Brand/Mystic Touch — things you always want at least one of) from `utility` (Battle Resurrection, Healthstones, Summoning Gateway, Demonic Gateway — fight-specific, no default requirement). The Coverage panel shows them as separate columns; Utility's column has a per-item minimum-count number input (`POST /api/raid-composition/utility-minimum`, saved to `raid_comp_utility_minimums`, officer-only) that feeds `improveCoverage` on the next Regenerate. `ALL_RAID_BUFFS`/`ALL_UTILITY`, `raidBuffDescription`/`utilityDescription`, and `allRaidBuffs`/`allUtility` are separate exports for the two categories; `computeTeamSummary`'s `raidBuffCounts`/`missingRaidBuffs` cover buffs only, `utilityCounts` covers utility (no "missing" list, since there's no universal minimum to compare against — that's per-item and officer-set).
- **Officer access**: `/admin` is admin-only (roster rank 0–2) except the paths in `OFFICER_ADMIN_PATHS` in `src/middleware.ts` (`/admin`, `/admin/log-matching`), which rank-3 officers can also open (`Astro.locals.isOfficer`, which includes admins). Officer modules are marked `officer: true` in `AdminLayout.astro`, and non-admin officers only see those tabs. Keep the two lists in sync. Note that `/raid-composition` and its `/api/raid-composition/*` mutation routes are deliberately **not** under `/admin` (it's a guild-member-facing Tools page) — they self-check `Astro.locals.isOfficer` in the route/page instead of relying on the `/admin` middleware gate.
- **Death Analysis defensive/consumable estimate**: expanding a raider's death list on `/death-analysis` also shows, per death, an estimated "defensives available" and "healthstone/potion" status as icon chips. This is computed lazily (not during the cron sync) by `src/lib/death-cooldowns.ts` — `GET /api/death-analysis/death-cooldowns` fetches `CombatantInfo` (for `specID`) and `Casts` events for that one death's window from WCL, finds each tracked ability's most recent cast before the death, and compares elapsed time to a hardcoded cooldown duration in `src/lib/defensive-cooldowns.ts`. Results are cached per-death in `death_cooldown_cache` (keyed like `death_analysis_events`) so a death is only queried once. Icons come from `src/lib/spell-icons.ts` (Blizzard's `/data/wow/media/spell/{id}` endpoint, cached in `spell_icon_cache`, mirroring `item_icon_cache`'s pattern in `upgrades.ts`) and are resolved fresh on every request rather than stored in the cooldown cache blob. This is an approximation — it ignores talent/gear cooldown reductions and treats "no cast seen in the pull" as available — and the reference table only covers personal defensives with long-stable ability IDs; `HEALTH_POTION` is intentionally left `null` (status "not_tracked", shown distinctly from a genuine "unknown") because potion ability IDs change every expansion/season and weren't verified for Midnight. Extend `CLASS_DEFENSIVES`/`SPEC_ID_TO_CLASS` as needed; don't add this work to `refreshDeathAnalysis`'s cron budget.
- **Mechanics Analysis**: `src/lib/mechanics-analysis.ts` backs `/mechanics-analysis` (Tools menu, guild members), gated on `FEATURE_FLAGS.mechanicsAnalysis` **and** `deathAnalysis`. It adds no report discovery of its own: it syncs only the canonical per-night reports from `getDeathAnalysisNights`. Bosses and mechanics are declared in `MECHANICS_BOSSES` (WCL encounter id plus debuff ability id). The first is Coiled Altar (3429) with Volatile Venom (1282419), where one `applydebuff` is one orb carried. `mechanics_analysis_reports` is a per (report, mechanic) cursor, so adding a mechanic backfills it. Player role comes from the CombatantInfo `specID` (`ROLE_BY_SPEC_ID`), so a new spec needs an entry there. The sync runs chained after death analysis in `/api/cron/refresh-logs`, with its own 6s budget, and is skipped if death analysis ran past 16s.
- **Warcraft Logs client**: `src/lib/wcl.ts` holds the shared WCL code — OAuth token, `queryWcl`, the rate-limit backoff (shared `site_settings` key), WCL-actor → roster-character matching (`loadWclCharacterLookup` + `matchWclActorCharId`, which trusts the actor's `gameID` — the Blizzard character id — when it's a known character and only falls back to name + realm; name-only matching mis-attributed stats when a deleted character's name was reused), and `fetchReportFightStats` (per-report presence + 4-death-capped death stats, scoped by a caller-supplied fight filter). Both `attendance.ts` and `death-analysis.ts` import from it. `raid-log-activity.ts` still has its own small token/query copy.
- **Useful Links page**: `/links` renders bookmarks tagged `wow` from the Tagstash public API (`src/lib/bookmarks.ts`, profile `JD`), cached 10 minutes at the Cloudflare edge. Links are curated in Tagstash, not in this repo — there is no admin CRUD and no D1 table behind the page (migration `0072` dropped `links`/`link_categories`). Filter chips are derived from each bookmark's other tags minus `NOISE_TAGS`.
- **Articles page**: `/articles` and the homepage's "Latest Articles" feed render posts tagged `world-of-warcraft` from a public Orboro.net API (`src/lib/orboro-posts.ts`, `GET https://orboro.net/api/posts/by-category/[slug]`), same fetch/cache shape as `bookmarks.ts` (10-minute edge cache). Content is authored and hosted entirely on Orboro.net — this repo has no article markdown, no content collection, and no D1 table for it. `/articles/:slug` doesn't render a post; it 301-redirects to the matching post's `url` from that same feed (rewrites to `/404` if the slug isn't found). Both pages carry a small credit link back to orboro.net — keep it if you touch that markup, per the guild's agreement with Orboro.
- A separate companion app (`HiddenLodgeDesktop`, different repo) consumes some of these API routes (e.g. `src/pages/api/desktop/raid-signups-today.ts`) — check before removing/gating an endpoint under `api/desktop/`.
- **Loot history raid allowlist**: `/api/desktop/loot-history` (ingest) and `/loot-history` (display) only accept/show entries whose `instance_name` matches `MIDNIGHT_TRACKED_RAID_NAMES` in `src/lib/midnight-tracked-raids.ts`. This list does not update itself — a new raid tier (new season) must be added here explicitly, or the Desktop app's uploads for that raid are silently dropped. Season 1 shipped without Season 2 ever being added, so no Venomous Abyss loot synced for the first month of Season 2 until this was caught and fixed.
